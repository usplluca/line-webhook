services:
  - type: web
    name: luca-webhook
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_VERSION
        value: 20.11.1
# ダミーコメント：Nodeバージョン強制指定の再確認用
# 再読み込みトリガーコメント
